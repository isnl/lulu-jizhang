大模型接入方案

通过nodejs简易的pdf文本识别，将识别到的内容传给大模型，走 JSON Output,

给大模型写一个让它将文本内容输出成 json 格式(符合入库的字段标准)的提示词，

AI输出后先进行预览，确认无误后

再将输出的json数据入库



APIKEY
sk-b4c8d41c1167486585824d2a8e741f8a

BASEURL
https://api.deepseek.com/v1

MODEL
deepseek-chat

DEEPSEEK 提供了标准的 JSON Output 功能


用户上传 PDF
    ↓
Cloudflare Function 接收文件
    ↓
pdf-parse 提取文本
    ↓
构造 Prompt（文本 + 指令）
    ↓
调用大模型 API (DEEPSEEK)
    ↓
解析 JSON 响应
    ↓
验证字段格式
    ↓
字段无误入库
    ↓
返回成功/失败